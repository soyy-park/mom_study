[project]
name = "quiz-capture"
version = "0.1.0"
description = "학습 영상 퀴즈 캡처 -> OCR -> DB -> 배포 파이프라인"
requires-python = ">=3.11"
dependencies = [
    "mss>=10.2",
    "pillow>=11",
    "requests>=2.31",
]

[tool.uv]
package = false
